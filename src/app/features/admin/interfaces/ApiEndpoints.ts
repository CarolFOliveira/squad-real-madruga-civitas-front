/**
 * ApiEndpoints
 *
 * Enum que representa os endpoints definidos pelo backend para as entidades.
 */
export enum ApiEndpoints {
  CLASSROOMS = 'turmas',
  STUDENTS = 'alunos',
  TEACHERS = 'professores',
}
